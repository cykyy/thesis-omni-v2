python create_manifest.py --output_dir /root/thesis/data/regspeech12_manifest --format tsv
```

**Expected output:**
```
/root/thesis/data/regspeech12_manifest/
├── train.tsv    (~80 hours)
├── valid.tsv    (~10 hours)
├── test.tsv
└── README.txt
